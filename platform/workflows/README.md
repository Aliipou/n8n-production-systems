# Platform workflow JSON

Export target for `[PLT]` workflows.

JSON files are not committed until they have been imported into the pinned n8n (`n8nio/n8n:2.37.9`), published, and have passed `platform/tests/e2e`. See `platform/docs/workflows.md` and AGENTS.md section 5.

Until that gate passes, this folder stays empty of workflow JSON.
